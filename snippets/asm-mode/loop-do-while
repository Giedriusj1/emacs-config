# -*- mode: snippet -*-
# name: do-while-loop-with-aliases
# key: loop-do
# --
${1:value} .req ${2:x0}
${3:limit} .req ${4:x1}

${5:loop}:
        ${6:// body}
        cmp $1, $3
        b.${7:lt} $5

        .unreq $1
        .unreq $3
